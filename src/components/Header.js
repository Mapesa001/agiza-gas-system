import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Agiza Gas System</h1>
      {/* Avoid duplicating buttons here if they're already in HomePage */}
    </header>
  );
};

export default Header;
